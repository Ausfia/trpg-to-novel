请根据以下跑团叙事素材，写出本卷的中文细节粗稿。

## 卷信息
- 卷号：vol{{ volume_index }}
- 工作标题：{{ working_title }}
- 主题摘要：{{ theme_summary }}
- 场景数：{{ scene_count }} 个
- 目标字数：约 {{ target_words }} 字（允许 ±20%）

## 本卷场景列表
{% for s in scenes %}
- `{{ s.id }}` [{{ s.session_id }}] {{ s.kind }} · {{ s.event_count }} 条事件
{% endfor %}

## 叙事素材
（格式说明：[DM] 是旁白/世界描写；[角色名] 是该角色说的话；[角色名·行动] 是角色的行动；[掷骰] 是该角色尝试某事并得到某结果。请保留叙事因果、动作意图和结果，不要保留规则数字。）

{{ narrative_text }}

---
**写作提示**：
1. 按场景顺序写，保持时间线清晰。每个 scene 开头放 `<!-- SCENE_BOUNDARY: scene_id -->`。
2. 每个 scene 写清：发生了什么、谁推动了变化、角色做了哪些关键动作/选择、冲突如何升级或解决、情绪位置是什么。
3. 对话场景：保留关键台词的声音和含义，可整理成小说对白；说明对关系、情绪、目标的影响。
4. 战斗场景：保留关键攻防、战术变化、姿态动作、受伤/失控/胜负结果；不要罗列机制数字，但不要把战斗压成一句概括。
5. 关键节拍优先：确保 key_beats 中的节点都有对应的叙事重量。
6. 这是供章节审稿和后续润色使用的细节粗稿，不要追求精修辞藻；重点是保留玩家行为、语言和选择，方便后续扩写成网文正文。

直接开始写细节粗稿（不要写"好的"或其他前缀，不要用 markdown 代码块包围）：
