请根据以下跑团叙事素材，写出本章的小说草稿。

## 章节信息
- 建议章节标题：{{ chapter_title }}
- 焦点角色：{{ focus_characters | join("、") }}
- 场景类型：{% for s in scenes %}{{ s.kind }}（{{ s.event_count }}条）{% if not loop.last %} → {% endif %}{% endfor %}

## 叙事素材
（格式说明：[DM]是旁白/世界描写；[角色名]是该角色说的话；[角色名·行动]是角色的行动；[掷骰]是该角色尝试了某事并得到了某结果——请用叙事语言表达因果，不要提数字。）

{{ narrative_text }}

---
**写作提示**：
1. 按场景顺序展开，保持时间线清晰。
2. 为每个主要场景选择一个情感焦点角色（可在章节内切换）。
3. 对话场景：在确保核心台词保留的前提下，增加神情、动作、内心独白。
4. 战斗场景：聚焦两三个关键时刻（而非每一击），用环境/人物状态的变化表达战况。
5. 结尾留白：章末不需要收束所有悬念，保留下一章的张力。

直接开始写正文（不要写"好的"或其他前缀，不要用 markdown 代码块包围）：
